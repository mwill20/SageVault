import streamlit as st
st.set_page_config(page_title="GuideBot Hello", layout="wide")
st.title("✅ GuideBot is alive")
st.write("If you can read this, rendering works. Next we’ll re-add the real app.")
