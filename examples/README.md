# Examples
- `fixtures/fastapi-mini/` — minimal repo; expect hits in `tests/` and `app/main.py` for “run tests”.
- `fixtures/api-excerpt/` — short API doc; expect hits for “auth header” and “rate limit”.

Run: `python examples/run_sample.py`
