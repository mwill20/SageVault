{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "pytest",
			"type": "shell",
			"command": "python -m pytest -q",
			"args": [
				"tests",
				"--maxfail=1",
				"--disable-warnings",
				"-q",
				"--audit-report-dir",
				"artifacts"
			]
		}
	]
}